<div class="gadget">
  <h2 class="star"><span>Публикации</span></h2>
  <div class="clr"></div>
  <ul class="sb_menu">
    <% @articles.each do |article| %>
      <li>
        <a <%= 'style="font-weight: bold"' if article[:id] == @a_id %> href="<%= article[:url] %>"><%= article[:title] %></a>
      </li>
    <% end %>
  </ul>
</div>
